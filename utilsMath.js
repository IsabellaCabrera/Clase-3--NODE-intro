export const sum =( a, b) => {
    return a + b; 
}; //modulo- funcion que voy a reutilizar en otro archivo 

 export const multiply = ( a, b) =>{
    return a * b
};// otro modulo 

//para exportar ambos modulos

// module.export = {
//     sum,
//     multiply,
// };esto de la manera vieja 
